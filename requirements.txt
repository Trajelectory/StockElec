# StockEleK — dépendances Python
# Installe avec : pip install -r requirements.txt
#
# Versions fixées avec == pour garantir la reproductibilité.
# Pour mettre à jour : pip install --upgrade <package> puis mettre à jour ici.

# Serveur web
flask==3.1.3
waitress==3.0.1

# Requêtes HTTP (enrichissement LCSC, Mouser, DigiKey, API JLCPCB)
requests==2.33.0

# Traitement d'images (EasyEDA PNG, redimensionnement)
Pillow==12.1.1

# Génération des fichiers KiCad
# Installe séparément et vérifie que la commande JLC2KiCadLib est dans le PATH :
# pip install JLC2KiCadLib
